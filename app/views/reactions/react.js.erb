<% if @reaction.errors.any? %>
  // $('#new-card-form').html("< escape_javascript(render partial: 'form')>");
  // flash message, error saving reaction!
<% else %>
  // increment reaction
  // $("< escape_javascript(render @reaction) >").appendTo("#reactions");
<% end %>
